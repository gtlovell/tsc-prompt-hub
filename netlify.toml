[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[build.processing]
  [build.processing.secrets]
    omit_keys = ["RESEND_API_KEY", "GEMINI_API_KEY"]
